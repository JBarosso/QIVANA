---
// ============================================
// AI QUIZ SETUP PAGE (DEPRECATED - Redirect to unified setup)
// ============================================
// ⚠️ Cette page est dépréciée. Le système est maintenant unifié dans setup.astro
// Redirection automatique vers la page unifiée

import { requireAuth } from '../../lib/auth';

// Protection de la route
const authResult = await requireAuth(Astro);

// Si c'est une redirection, elle a déjà été effectuée
if (authResult instanceof Response) {
  return authResult;
}

// Rediriger vers la page unifiée
return Astro.redirect('/quiz/setup');
