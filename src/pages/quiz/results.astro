---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { requireAuth } from '../../lib/auth';
import { getQuizSession, completeQuizSession } from '../../lib/quiz';
import type { Answers, Question } from '../../types';
import type { SupabaseClient } from '@supabase/supabase-js';
import type { Database } from '../../types/supabase';

// Protection de la route
const authResult = await requireAuth(Astro);

// Si c'est une redirection, elle a déjà été effectuée
if (authResult instanceof Response) {
  return authResult;
}

const { user, profile } = authResult;

// Récupérer l'ID de session
const sessionId = Astro.url.searchParams.get('session');

if (!sessionId) {
  return Astro.redirect('/quiz/setup');
}

// Cast pour accéder à supabase
const supabase = (Astro.locals as { supabase?: SupabaseClient<Database> }).supabase;
if (!supabase) {
  return Astro.redirect('/quiz/setup');
}

// Charger la session
const session = await getQuizSession(supabase, sessionId);

if (!session) {
  return Astro.redirect('/quiz/setup');
}

// Vérifier que la session appartient à l'utilisateur
if (session.user_id !== user.id) {
  return Astro.redirect('/quiz/setup');
}

// Si pas encore terminée, marquer comme complétée
if (!session.completed_at) {
  await completeQuizSession(supabase, sessionId);
}

// Charger les questions pour afficher les détails
// ⚠️ Pour le mode custom-quiz, les questions n'existent pas en DB (IDs temporaires)
let questions: Question[] | null = null;

if (session.quiz_type !== 'ai-custom-quiz') {
  const { data: questionsData } = await supabase
    .from('questions')
    .select('*')
    .in('id', session.questions_ids);
  
  // Convertir les données Supabase (Json) en Question[] (string[])
  if (questionsData) {
    questions = questionsData.map((q) => ({
      ...q,
      choices: Array.isArray(q.choices) ? q.choices as string[] : [],
    })) as Question[];
  }
}

// Calculer les stats
const totalQuestions = session.questions_ids.length;
const answers = (session.answers as Answers) || [];
const answeredQuestions = answers.filter((a) => a !== null && a !== undefined).length;

// Pour les correctAnswers, on ne peut pas les calculer précisément pour custom-quiz car les questions ne sont pas en DB
// On utilise le score comme proxy
let correctAnswers = 0;

if (session.quiz_type === 'ai-custom-quiz') {
  // Estimation basée sur score (10 pts/question max)
  correctAnswers = Math.round(session.score / 10);
} else {
  // Pour les quiz normaux, vérifier chaque réponse
  // On s'assure que toutes les questions sont chargées avant de calculer
  if (questions && questions.length === session.questions_ids.length) {
    correctAnswers = answers.reduce((count, answer, index) => {
      if (answer === null || answer === undefined) return count;
      const questionId = session.questions_ids[index];
      const question = questions.find((q: Question) => q.id === questionId);
      // Si la question n'est pas trouvée, on ne la compte pas (ne devrait pas arriver)
      if (!question) {
        console.warn(`Question ${questionId} not found for index ${index}`);
        return count;
      }
      if (answer === question.correct_index) {
        return count + 1;
      }
      return count;
    }, 0);
  } else {
    // Fallback : si les questions ne sont pas toutes chargées, utiliser le score comme estimation
    console.warn('Not all questions loaded, using score estimation');
    correctAnswers = Math.round(session.score / 10);
  }
}

const incorrectAnswers = answeredQuestions - correctAnswers;
const accuracy = totalQuestions > 0 ? Math.round((correctAnswers / totalQuestions) * 100) : 0;
const finalScore = Math.round(session.score * 100) / 100;

// Déterminer le rank basé sur le score
let rank = '🥉 Bronze';
let rankClass = 'bronze';
if (accuracy >= 90) {
  rank = '🏆 Légende';
  rankClass = 'legend';
} else if (accuracy >= 75) {
  rank = '🥇 Or';
  rankClass = 'gold';
} else if (accuracy >= 50) {
  rank = '🥈 Argent';
  rankClass = 'silver';
}
---

<BaseLayout title="Résultats du Quiz - Qivana">
  <div class="quiz-results">
    <div class="quiz-results__container">
      {/* Header avec rank */}
      <div class={`quiz-results__header quiz-results__header--${rankClass}`}>
        <div class="quiz-results__rank">{rank}</div>
        <h1 class="quiz-results__title">Quiz Terminé !</h1>
        <p class="quiz-results__subtitle">
          {session.universe.toUpperCase()} • {session.difficulty === 'easy' ? 'Facile' : session.difficulty === 'medium' ? 'Moyen' : 'Difficile'}
        </p>
      </div>

      {/* Score principal */}
      <div class="quiz-results__score-card">
        <div class="quiz-results__score-value">{finalScore}</div>
        <div class="quiz-results__score-label">Points totaux</div>
      </div>

      {/* Stats grid */}
      <div class="quiz-results__stats">
        <div class="stat-card stat-card--success">
          <div class="stat-card__icon">✓</div>
          <div class="stat-card__value">{correctAnswers}</div>
          <div class="stat-card__label">Bonnes réponses</div>
        </div>

        <div class="stat-card stat-card--danger">
          <div class="stat-card__icon">✗</div>
          <div class="stat-card__value">{incorrectAnswers}</div>
          <div class="stat-card__label">Mauvaises réponses</div>
        </div>

        <div class="stat-card stat-card--primary">
          <div class="stat-card__icon">🎯</div>
          <div class="stat-card__value">{accuracy}%</div>
          <div class="stat-card__label">Précision</div>
        </div>
      </div>

      {/* Messages motivants */}
      <div class="quiz-results__message">
        {accuracy >= 90 && (
          <p>🔥 Incroyable ! Tu es un véritable expert de {session.universe} !</p>
        )}
        {accuracy >= 75 && accuracy < 90 && (
          <p>👏 Excellent travail ! Continue comme ça !</p>
        )}
        {accuracy >= 50 && accuracy < 75 && (
          <p>💪 Pas mal ! Encore un peu de pratique et ce sera parfait !</p>
        )}
        {accuracy < 50 && (
          <p>😊 Bon début ! Recommence pour améliorer ton score !</p>
        )}
      </div>

      {/* Actions */}
      <div class="quiz-results__actions">
        <a href="/quiz/setup" class="btn btn--primary btn--large">
          Nouveau Quiz 🚀
        </a>
        <a href="/profile" class="btn btn--secondary btn--large">
          Mon Profil
        </a>
      </div>

      {/* Share (optionnel pour plus tard) */}
      <div class="quiz-results__share">
        <p class="quiz-results__share-label">Partage ton score !</p>
        <div class="quiz-results__share-buttons">
          <button class="share-btn share-btn--twitter" disabled>
            🐦 Twitter
          </button>
          <button class="share-btn share-btn--discord" disabled>
            💬 Discord
          </button>
        </div>
        <p class="quiz-results__share-note">(Bientôt disponible)</p>
      </div>
    </div>
  </div>
</BaseLayout>

<style lang="scss">
@use '../../styles/pages/quiz' as *;
</style>
